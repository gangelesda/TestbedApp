<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.android.zigbeetestbed">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:fullBackupContent="false"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuActivity"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SendMenuActivity"
            android:label="@string/title_activity_send_menu" />
        <activity
            android:name=".DisplayMenuActivity"
            android:label="@string/title_display_menu" />
        <activity
            android:name=".TemperatureArduinoActivity"
            android:label="@string/title_temp_arduino"/>
        <activity
            android:name=".MotionActivity"
            android:label="@string/title_motion"/>
        <activity
            android:name=".CameraActivity"
            android:label="@string/title_camera"/>
        <activity
            android:name=".DoorlockActivity"
            android:label="@string/title_accel"/>
        <activity
            android:name=".RegisterActivity"
            android:label="@string/register"/>
    </application>


</manifest>